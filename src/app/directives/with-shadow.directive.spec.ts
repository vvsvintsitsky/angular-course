import { WithShadowDirective } from './with-shadow.directive';

describe('WithShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new WithShadowDirective();
    expect(directive).toBeTruthy();
  });
});
