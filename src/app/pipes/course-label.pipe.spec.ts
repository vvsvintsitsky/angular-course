import { CourceLabelPipe } from './course-label.pipe';

describe('CourceLabelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CourceLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
